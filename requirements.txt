# The extra index makes pip resolve CPU-only torch (torch-x.y+cpu, ~200 MB)
# instead of the CUDA build with its multi-GB nvidia-* dependencies —
# required to fit Streamlit Community Cloud's build and memory limits.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.0.0
openpyxl>=3.1.0

# NLP fallback in src/classification.py (zero-shot merchant classification).
# The code degrades gracefully to rules-only if these are missing.
torch
transformers>=4.35.0

# --- Deferred until the modules that need them land ---
# scikit-learn>=1.3.0
# Production database (MVP uses SQLite, stdlib):
# supabase>=2.0.0
# python-dotenv>=1.0.0
